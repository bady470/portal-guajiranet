import { TestBed } from '@angular/core/testing';
import { AppComponent } from './main';

describe('Portal Guajiranet frontend', () => {
  it('debe tener un componente raíz definido', () => {
    expect(AppComponent).toBeDefined();
  });
});
