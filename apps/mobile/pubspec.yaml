name: portal_guajiranet_mobile
description: Aplicación mobile Flutter del Portal Guajiranet.
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
